# Ruby program to demonstrate
puts "Bitwise Operators"

# variables
a = 14
b = 20

puts "Bitwise AND operator:#{(a & b)}"

puts "Bitwise OR operator:#{(a |b)}"

puts "Bitwise XOR operator:#{(a ^ b)}"

puts "Bitwise Complement operator:#{(~a)}"

puts "Binary right shift operator:#{(a >> 2)}"

puts "Binary left shift operator:#{(a << 2)}"